# EventPilot Frontend

React SPA migrated from the original Next.js public UI.

## Run

```bash
cd frontend
pnpm install
cp .env.example .env
pnpm dev
```

The app runs on `http://localhost:5173` and calls the Spring Boot backend at
`VITE_API_BASE_URL`.
